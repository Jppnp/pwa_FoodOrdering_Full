First of all, please run this command

`npm install`

Next,

`cd Backend && docker build -t golang-api .`
 and make sure you can see docker image name "golang-api".

Then,

`cd .. && cd FrontEnd && docker build -t pwa_react .` 

and make sure you can see a new docker image name "pwa_react".
So, last image please rum

 `docker pull postgres`

Now you can run 

`docker compose up -d` 
